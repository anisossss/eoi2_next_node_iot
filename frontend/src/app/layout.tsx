import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CSIR IoT Weather Dashboard',
  description: 'Real-time weather and IoT sensor monitoring dashboard for CSIR - Council for Scientific and Industrial Research',
  keywords: ['CSIR', 'IoT', 'Weather', 'Dashboard', 'Sensors', 'Real-time'],
  authors: [{ name: 'CSIR Software Development Team' }],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.className} antialiased`}>
        <div className="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100 dark:from-slate-900 dark:to-slate-800">
          {children}
        </div>
      </body>
    </html>
  )
}
